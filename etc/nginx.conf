server {
    listen 80 default;
    #error_log /home/box/web/etc/nginx.erro.log;

    location ^~ /uploads/ {
        root /home/box/web;
    }

    location ~* ^.+\.\w+$ {
        root /home/box/web/public;
    }

    location / {
        return 404;
    }

}
